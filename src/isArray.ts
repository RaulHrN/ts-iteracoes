const sobrenomes: string[] = ["Nascimento", "Moreira", "Souza"];

const varNome: string = "Nascimento";

//Verifica se o Elemento é um array//

console.log(Array.isArray(sobrenomes));
console.log(Array.isArray(varNome));