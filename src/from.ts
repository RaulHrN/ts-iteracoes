//Cria um array dos elementos dentro do parenteses || Caso for mais de 1 elemento, for usar números ou não quiser separar a string, utilizar []//

console.log(Array.from("Raul"));

console.log(Array.from(["Raul"]));

console.log(Array.from([1,2,3], v => v*v));